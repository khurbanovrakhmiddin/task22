# .github/workflows/release-and-notify.yml
name: Release and Telegram Notification

on:
  push:
    branches: [ main, master ]
    tags: [ 'v*' ]

env:
  FLUTTER_VERSION: '3.19.5'

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: ${{ env.FLUTTER_VERSION }}
        cache: true

    - name: Install dependencies
      run: flutter pub get

    - name: Verify formatting
      run: flutter format --set-exit-if-changed .

    - name: Analyze code
      run: flutter analyze

    - name: Run tests
      run: flutter test

    - name: Build APK
      run: flutter build apk --release --split-per-abi
      
    - name: Build AppBundle
      run: flutter build appbundle --release

    - name: Get version info
      id: version
      run: |
        echo "APP_NAME=$(flutter pub get && flutter pub run flutter_launcher_name:main | grep -o 'app_name: .*' | cut -d' ' -f2)" >> $GITHUB_OUTPUT
        echo "VERSION=$(flutter pub run flutter_version:main | grep -o 'version: .*' | cut -d' ' -f2)" >> $GITHUB_OUTPUT
        echo "BUILD_NUMBER=$(flutter pub run flutter_version:main | grep -o 'build_number: .*' | cut -d' ' -f2)" >> $GITHUB_OUTPUT

    - name: Create Release
      id: create_release
      uses: softprops/action-gh-release@v1
      if: startsWith(github.ref, 'refs/tags/')
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref_name }}
        release_name: Release ${{ github.ref_name }}
        body: |
          🚀 Новый релиз ${{ github.ref_name }}
          
          📱 **Приложение:** ${{ steps.version.outputs.APP_NAME }}
          📦 **Версия:** ${{ steps.version.outputs.VERSION }}
          🔢 **Build:** ${{ steps.version.outputs.BUILD_NUMBER }}
          
          📅 **Дата сборки:** ${{ github.event.head_commit.timestamp }}
          📝 **Коммит:** ${{ github.sha }}
          
          **Изменения:**
          ${{ github.event.head_commit.message }}
        draft: false
        prerelease: false
        files: |
          build/app/outputs/flutter-apk/*.apk
          build/app/outputs/bundle/release/*.aab

    - name: Send Telegram Notification
      uses: appleboy/telegram-action@master
      with:
        to: ${{ secrets.TELEGRAM_CHAT_ID }}
        token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        message: |
          🚀 *Новый релиз!*
          
          📱 *Приложение:* ${{ steps.version.outputs.APP_NAME }}
          🏷️ *Версия:* ${{ steps.version.outputs.VERSION }} (${{ steps.version.outputs.BUILD_NUMBER }})
          📅 *Собрано:* ${{ github.event.head_commit.timestamp }}
          
          📝 *Коммит:*
          `${{ github.event.head_commit.message }}`
          
          👤 *Автор:* ${{ github.event.head_commit.author.name }}
          
          📦 *Файлы:*
          {{range steps.create_release.outputs.assets}}• [{{.name}}]({{.url}})
          {{end}}
          
          🔗 *Релиз:* https://github.com/${{ github.repository }}/releases/tag/${{ github.ref_name }}
        format: markdown

  notify-push:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && !startsWith(github.ref, 'refs/tags/')
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: ${{ env.FLUTTER_VERSION }}

    - name: Get version info
      id: version
      run: |
        echo "APP_NAME=$(grep -o 'app_name: .*' pubspec.yaml | head -1 | cut -d' ' -f2)" >> $GITHUB_OUTPUT
        echo "VERSION=$(grep 'version:' pubspec.yaml | cut -d' ' -f2)" >> $GITHUB_OUTPUT

    - name: Send Telegram Push Notification
      uses: appleboy/telegram-action@master
      with:
        to: ${{ secrets.TELEGRAM_CHAT_ID }}
        token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        message: |
          📥 *Новый пуш в репозиторий*
          
          📱 *Приложение:* ${{ steps.version.outputs.APP_NAME }}
          🏷️ *Версия:* ${{ steps.version.outputs.VERSION }}
          🌿 *Ветка:* ${{ github.ref_name }}
          
          📝 *Коммит:*
          `${{ github.event.head_commit.message }}`
          
          👤 *Автор:* ${{ github.event.head_commit.author.name }}
          
          🔗 *Ссылка:* https://github.com/${{ github.repository }}/commit/${{ github.sha }}
        format: markdown
